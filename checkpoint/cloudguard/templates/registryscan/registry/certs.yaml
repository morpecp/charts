{{- $config := fromYaml (include "registryscan.registry.config" .) -}}
{{- if $config.featureConfig.enabled -}}
{{- $cert := fromYaml (include "generate.selfsigned.cert" $config) -}}
{{ template "registryscan.registry.secret" dict "config" $config "key" $cert.key }}
---
{{ template "registryscan.registry.configmap" dict "config" $config "crt" $cert.crt }}
{{- end -}}